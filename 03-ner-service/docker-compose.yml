services:
  api:
    build:
      context: .
    ports:
      - "8000:8000"

  demo:
    build:
      context: .
    ports:
      - "8501:8501"
    command: ["streamlit", "run", "src/demo/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
