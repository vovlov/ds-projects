FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml .
RUN uv pip install --system --no-cache \
    fastapi uvicorn streamlit

COPY ner/ ner/

EXPOSE 8000
CMD ["uvicorn", "ner.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
