name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  test-churn:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra churn
      - run: cd 01-customer-churn-mlops && uv run python -m pytest tests/ -v --tb=short

  test-rag:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra rag
      - run: cd 02-rag-enterprise && uv run python -m pytest tests/ -v --tb=short

  test-ner:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra ner
      - run: cd 03-ner-service && uv run python -m pytest tests/ -v --tb=short

  test-fraud:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra fraud
      - run: PYTHONPATH=04-graph-fraud-detection uv run python -m pytest 04-graph-fraud-detection/tests/ -v --tb=short

  test-anomaly:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra anomaly
      - run: PYTHONPATH=05-realtime-anomaly uv run python -m pytest 05-realtime-anomaly/tests/ -v --tb=short

  test-cv:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra cv
      - run: PYTHONPATH=06-cv-document-scanner uv run python -m pytest 06-cv-document-scanner/tests/ -v --tb=short

  test-pricing:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra pricing
      - run: PYTHONPATH=07-realestate-pricing uv run python -m pytest 07-realestate-pricing/tests/ -v --tb=short

  test-review:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra review
      - run: PYTHONPATH=08-llm-code-review uv run python -m pytest 08-llm-code-review/tests/ -v --tb=short

  test-recsys:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra recsys
      - run: PYTHONPATH=09-recsys-feature-store uv run python -m pytest 09-recsys-feature-store/tests/ -v --tb=short

  test-quality:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - run: uv sync --extra dev --extra quality
      - run: PYTHONPATH=10-data-quality-platform uv run python -m pytest 10-data-quality-platform/tests/ -v --tb=short
