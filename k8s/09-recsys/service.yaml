apiVersion: v1
kind: Service
metadata:
  name: recsys-api
  namespace: ds-projects
  labels:
    app: recsys-api
spec:
  selector:
    app: recsys-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
