apiVersion: v1
kind: Service
metadata:
  name: ner-api
  namespace: ds-projects
  labels:
    app: ner-api
spec:
  selector:
    app: ner-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
