apiVersion: v1
kind: Service
metadata:
  name: churn-api
  namespace: ds-projects
  labels:
    app: churn-api
spec:
  selector:
    app: churn-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
