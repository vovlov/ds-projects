apiVersion: v1
kind: Service
metadata:
  name: scanner-api
  namespace: ds-projects
  labels:
    app: scanner-api
spec:
  selector:
    app: scanner-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
