# Template — copy to secrets.yaml and fill in values.
# Never commit secrets.yaml to git (it is gitignored).
# For production use an external secrets operator (e.g. External Secrets + AWS Secrets Manager).
apiVersion: v1
kind: Secret
metadata:
  name: ds-projects-secrets
  namespace: ds-projects
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  MLFLOW_S3_ACCESS_KEY: "REPLACE_ME"
  MLFLOW_S3_SECRET_KEY: "REPLACE_ME"
