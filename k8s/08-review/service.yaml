apiVersion: v1
kind: Service
metadata:
  name: review-api
  namespace: ds-projects
  labels:
    app: review-api
spec:
  selector:
    app: review-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
